name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Check if version changed
        id: version_check
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        run: |
          CURRENT=$(grep -oP '(?<=<Version>).*(?=</Version>)' Directory.Build.props)
          PREVIOUS=$(git show HEAD~1:Directory.Build.props | grep -oP '(?<=<Version>).*(?=</Version>)' || echo "none")
          echo "Current version: $CURRENT"
          echo "Previous version: $PREVIOUS"
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          if [ "$CURRENT" != "$PREVIOUS" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test
        run: dotnet test --no-build --configuration Release --verbosity normal

      - name: Pack
        if: steps.version_check.outputs.changed == 'true'
        run: dotnet pack --no-build --configuration Release --output ./nupkgs

      - name: Push to NuGet
        if: steps.version_check.outputs.changed == 'true'
        run: dotnet nuget push ./nupkgs/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Tag commit
        if: steps.version_check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag ${{ steps.version_check.outputs.current }}
          git push origin ${{ steps.version_check.outputs.current }}